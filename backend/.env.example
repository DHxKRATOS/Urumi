# Server Configuration
PORT=5000
NODE_ENV=development

# Kubernetes Configuration
KUBECONFIG=~/.kube/config

# Helm Configuration
HELM_CHART_PATH=../helm-charts/woocommerce-store

# Store Configuration
DEFAULT_INGRESS_DOMAIN=localhost
